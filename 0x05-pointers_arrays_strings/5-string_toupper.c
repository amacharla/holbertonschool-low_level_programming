/**
 * string_toupper - converts lowercase to uppercase
 * @s: pointer of type char array
 * Return: s which is uppercase
 */
char *string_toupper(char *s)
{
	int i;

	for (i = 0; s[i]; i++)
	{
		if (s[i] >= 'a' && s[i] <= 'z')
			s[i] = s[i] - 32;
	}
	return (s);
}
