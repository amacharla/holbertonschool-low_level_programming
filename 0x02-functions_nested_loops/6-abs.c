#include "holberton.h"
/**
 * main - check the code for Holberton School students.
 * returns absolute value
 * Return: Always 0.
 */
int _abs(int abs)
{
	if (abs < 0)
		return (-abs);
	else
		return (abs);
}
